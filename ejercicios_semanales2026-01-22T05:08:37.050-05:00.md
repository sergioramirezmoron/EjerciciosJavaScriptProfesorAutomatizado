### Ejercicios Prácticos de JavaScript
#### Ejercicio 1: Obtener valor de propiedad "pais"
Escribe una función que tome un objeto como argumento y devuelva el valor de la propiedad con clave "pais". Si la clave no existe, devuelve "No se encuentra".

#### Ejercicio 2: Comprobar existencia de propiedad
Escribe una función que tome un objeto y una cadena como argumentos. Devuelve `true` si el objeto tiene una propiedad con la clave igual a la cadena, y `false` en caso contrario.

#### Ejercicio 3: Comprobar propiedad con valor verdadero
Escribe una función que tome un objeto y una cadena como argumentos. Devuelve `true` si el objeto tiene una propiedad con la clave igual a la cadena y su valor es verdadero (no nulo, indefinido o falso), y `false` en caso contrario.